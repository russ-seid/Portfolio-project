---
phase: 02-landing-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - portfolio/src/app/globals.css
  - portfolio/src/app/layout.tsx
  - portfolio/src/app/page.tsx
  - portfolio/src/components/layout/Nav.tsx
  - portfolio/src/components/layout/Footer.tsx
  - portfolio/src/components/sections/HeroSection.tsx
  - portfolio/src/components/sections/AboutSection.tsx
  - portfolio/src/components/sections/WorkSection.tsx
  - portfolio/src/components/ui/CaseStudyCard.tsx
  - portfolio/src/types/index.ts
autonomous: false
requirements:
  - NAV-01
  - LAND-01
  - LAND-02
  - LAND-03
  - LAND-04
  - DESIGN-01
  - DESIGN-02
  - DESIGN-03
  - DESIGN-04

must_haves:
  truths:
    - "Hero section displays the exact headline and tagline text"
    - "About section communicates background, company, and interests"
    - "Four case study cards are visible with correct titles, descriptions, and links"
    - "Footer shows contact email and LinkedIn links"
    - "Navigation bar is visible at top with name"
    - "Layout is correct on mobile (375px) and desktop (1280px)"
    - "No animations, transitions, or framer-motion imports exist anywhere"
  artifacts:
    - path: "portfolio/src/app/globals.css"
      provides: "CSS custom properties for color tokens, spacing, typography"
      contains: "--color-bg"
    - path: "portfolio/src/app/layout.tsx"
      provides: "Inter font via next/font/google, applied to html element"
      contains: "Inter"
    - path: "portfolio/src/components/layout/Nav.tsx"
      provides: "Sticky top nav with name link"
      min_lines: 10
    - path: "portfolio/src/components/sections/HeroSection.tsx"
      provides: "Hero headline and tagline"
      contains: "I turn complexity into clarity"
    - path: "portfolio/src/components/sections/AboutSection.tsx"
      provides: "About heading and body paragraph"
      contains: "Product Designer"
    - path: "portfolio/src/components/sections/WorkSection.tsx"
      provides: "Work heading and 4 CaseStudyCard instances"
      contains: "CaseStudyCard"
    - path: "portfolio/src/components/ui/CaseStudyCard.tsx"
      provides: "Card component with title, description, link"
      exports: ["CaseStudyCard"]
    - path: "portfolio/src/components/layout/Footer.tsx"
      provides: "Contact and social links"
      contains: "linkedin"
    - path: "portfolio/src/app/page.tsx"
      provides: "Home page composing all sections"
      contains: "HeroSection"
  key_links:
    - from: "portfolio/src/app/page.tsx"
      to: "portfolio/src/components/sections/HeroSection.tsx"
      via: "import and render"
      pattern: "import.*HeroSection"
    - from: "portfolio/src/components/sections/WorkSection.tsx"
      to: "portfolio/src/components/ui/CaseStudyCard.tsx"
      via: "import and map over case studies"
      pattern: "import.*CaseStudyCard"
    - from: "portfolio/src/components/ui/CaseStudyCard.tsx"
      to: "/case-studies/*"
      via: "Next.js Link href"
      pattern: "href.*case-studies"
    - from: "portfolio/src/app/layout.tsx"
      to: "portfolio/src/components/layout/Nav.tsx"
      via: "import and render in body"
      pattern: "import.*Nav"
---

<objective>
Implement the complete landing page: design tokens, Inter font, Nav, Hero, About, Work (with 4 case study cards), and Footer.

Purpose: Deliver the full landing page so a visitor can see who Russ is, read about his background, browse four case study cards, and find contact links.
Output: 10 files (globals.css update, layout.tsx update, 6 new components, page.tsx rewrite, types update). The site renders the complete landing page at localhost:3000.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-landing-page/02-UI-SPEC.md
@.planning/phases/01-foundation/01-01-SUMMARY.md

<interfaces>
<!-- Existing codebase contracts the executor needs -->

From portfolio/src/app/layout.tsx:
```typescript
// Current: no font, plain body wrapper
export default function RootLayout({ children }: Readonly<{ children: React.ReactNode }>) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
```

From portfolio/src/app/globals.css:
```css
@import "tailwindcss";
:root {
  --background: #ffffff;
  --foreground: #171717;
}
body {
  background: var(--background);
  color: var(--foreground);
  font-family: Arial, Helvetica, sans-serif;
}
```

From portfolio/src/types/index.ts:
```typescript
// Empty barrel file — ready for type definitions
```

From Next.js 16 font API (per node_modules/next/dist/docs):
```typescript
import { Inter } from 'next/font/google'
const inter = Inter({ subsets: ['latin'], display: 'swap' })
// Apply via className={inter.className} on <html>
```

From Next.js 16 Link API:
```typescript
import Link from 'next/link'
// <Link href="/path">text</Link> — no nested <a> needed
```
</interfaces>

**IMPORTANT — Next.js 16 breaking changes:**
The project uses Next.js 16.2.1. Before writing code, read the relevant guide in `node_modules/next/dist/docs/` if unsure about any API. Key points confirmed from docs:
- Font: `import { Inter } from 'next/font/google'` with `inter.className` on html element
- Link: `<Link href="...">` directly, no nested `<a>`
- App Router: all pages under `src/app/`

**Tailwind CSS v4 — CSS-based config:**
- NO `tailwind.config.ts` file exists or should be created
- Custom values defined as CSS custom properties in globals.css
- Use `@theme` block in globals.css for Tailwind to pick up custom tokens
- Utility classes work as normal: `text-[color]`, `p-[size]`, etc.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Design system tokens, Inter font, and layout shell</name>
  <files>
    portfolio/src/app/globals.css,
    portfolio/src/app/layout.tsx,
    portfolio/src/components/layout/Nav.tsx,
    portfolio/src/components/layout/Footer.tsx,
    portfolio/src/types/index.ts
  </files>
  <read_first>
    portfolio/src/app/globals.css,
    portfolio/src/app/layout.tsx,
    portfolio/src/types/index.ts,
    .planning/phases/02-landing-page/02-UI-SPEC.md (Design System, Spacing Scale, Typography, Color, Layout Spec sections),
    portfolio/node_modules/next/dist/docs/01-app/03-api-reference/02-components/font.md (first 60 lines — confirm Inter import pattern for Next.js 16)
  </read_first>
  <action>
**types/index.ts** — Define CaseStudy type:
```typescript
export interface CaseStudy {
  title: string;
  description: string;
  href: string;
}
```

**globals.css** — Replace current content with design tokens per UI-SPEC. Keep `@import "tailwindcss"` at top. Add CSS custom properties under `:root`:
- `--color-bg: #ffffff`
- `--color-bg-secondary: #f5f5f5`
- `--color-text: #171717`
- `--color-text-muted: #737373`
- `--color-border: #e5e5e5`
- `--color-card-hover: #ebebeb`
- `--color-card-active: #e0e0e0`

Body styles: `background: var(--color-bg); color: var(--color-text);` — remove the `font-family` line (Inter will be applied via className on html element, not CSS).

No `@theme` block needed — we will use arbitrary values in Tailwind classes referencing these CSS vars (e.g., `bg-[var(--color-bg-secondary)]`) or use Tailwind's built-in neutral palette directly where values match (e.g., `text-neutral-500` for muted). The executor should prefer direct Tailwind classes when the value matches a Tailwind default (e.g., `bg-neutral-100` for #f5f5f5, `text-neutral-800` for near-black) and use CSS vars for hover states and borders.

**layout.tsx** — Update to:
1. Import Inter from `next/font/google` with `subsets: ['latin'], display: 'swap'`
2. Apply `inter.className` to the `<html>` element
3. Import and render `<Nav />` inside `<body>` above `{children}`
4. Import and render `<Footer />` inside `<body>` below `{children}`
5. Keep existing metadata

Per DESIGN-01: No animations. Per DESIGN-03: components accept optional `className` prop.

**Nav.tsx** — Create sticky top navigation (per UI-SPEC Layout Spec):
- Semantic `<nav>` element, sticky top-0 z-50, white background, height 56px
- Full viewport width background, but inner content constrained to `max-w-[1080px] mx-auto`
- Left side: Link to "/" with text "Russ Seidametov" in semibold (font-semibold), color `#171717`
- Hover on name: opacity 70%. Active: opacity 50%. Focus: outline 2px solid #171717 offset 2px
- No right-side links in v1 (single-scroll portfolio)
- Horizontal padding: px-4 on mobile, sm:px-16 on desktop (16px / 64px)
- Accept optional `className` prop
- Use `import Link from 'next/link'` for the name link

**Footer.tsx** — Create minimal footer (per UI-SPEC):
- Semantic `<footer>` element
- Content constrained to `max-w-[1080px] mx-auto`
- Padding: py-8 top/bottom (32px), same horizontal padding as Nav (px-4 sm:px-16)
- Top border: 1px solid #E5E5E5 (border-t border-neutral-200)
- "Get in touch" label in body size
- Two links: email (mailto:rustem4994@gmail.com — use this as placeholder, executor can note it for user to confirm) and LinkedIn (https://linkedin.com/in/russseid — placeholder href)
- Link color: muted (#737373), hover color: #171717. Focus: outline 2px solid #171717 offset 2px
- Accept optional `className` prop
- Flex layout: label on left, links on right (row on desktop, column on mobile)
  </action>
  <verify>
    <automated>cd portfolio && npx tsc --noEmit && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - globals.css has all 7 CSS custom properties defined under :root
    - layout.tsx imports Inter from next/font/google and applies inter.className to html element
    - layout.tsx renders Nav above children and Footer below children
    - Nav.tsx exports a functional component with sticky positioning, name link to "/", 56px height, 1080px max-width content
    - Footer.tsx exports a functional component with "Get in touch" text, email and LinkedIn links, top border
    - types/index.ts exports CaseStudy interface with title, description, href fields
    - TypeScript compiles with zero errors
    - Production build succeeds
    - No framer-motion imports, no CSS transitions, no animation classes anywhere
  </acceptance_criteria>
  <done>Design tokens in globals.css, Inter font on html, Nav and Footer rendered in layout, CaseStudy type exported, build passes clean</done>
</task>

<task type="auto">
  <name>Task 2: Hero, About, Work sections and CaseStudyCard component</name>
  <files>
    portfolio/src/components/sections/HeroSection.tsx,
    portfolio/src/components/sections/AboutSection.tsx,
    portfolio/src/components/sections/WorkSection.tsx,
    portfolio/src/components/ui/CaseStudyCard.tsx,
    portfolio/src/app/page.tsx
  </files>
  <read_first>
    .planning/phases/02-landing-page/02-UI-SPEC.md (Typography, Layout Spec, Component Inventory, Copywriting Contract, Interaction States, Accessibility Contract sections),
    portfolio/src/types/index.ts (for CaseStudy type — created in Task 1),
    portfolio/src/components/layout/Nav.tsx (for consistent styling patterns established in Task 1),
    portfolio/node_modules/next/dist/docs/01-app/03-api-reference/02-components/link.md (first 50 lines — confirm Link usage for Next.js 16)
  </read_first>
  <action>
**HeroSection.tsx** — Per UI-SPEC Layout Spec and Copywriting Contract:
- Semantic `<section>` with `aria-label="Hero"`
- Padding: pt-24 pb-16 (96px top, 64px bottom) — per layout spec
- Content constrained to `max-w-[1080px] mx-auto`, horizontal padding px-4 sm:px-16
- `<h1>` with exact text: "Russ Seidametov. I turn complexity into clarity."
  - Display typography: text-[28px] sm:text-[40px], font-semibold, leading-[1.1], tracking-tight
- `<p>` tagline below: "Currently at Fiyrpod, designing athlete tracking tools that make elite-level performance analysis accessible to every coach."
  - Label typography: text-[13px], font-normal, leading-[1.4], text-neutral-500 (muted color #737373)
  - Spacing: mt-4 (16px gap below headline)
- Accept optional `className` prop

**AboutSection.tsx** — Per UI-SPEC:
- Semantic `<section>` with `aria-label="About"`
- Padding: py-12 (48px top and bottom)
- Content constrained to `max-w-[1080px] mx-auto`, horizontal padding px-4 sm:px-16
- `<h2>` with text "About" — Heading typography: text-[20px], font-semibold, leading-[1.3]
- `<p>` body text — exact copy from UI-SPEC Copywriting Contract:
  "I'm a Product Designer specializing in sports tech and data-heavy products. At Fiyrpod I design GPS analytics and athlete monitoring tools. Previously at Breakthru, where I led the product design through a B2C to B2B transition. I'm drawn to 0->1 problems -- the stage where research, systems thinking, and craft compound. Outside work: surfing, board games, soccer, guitar, photography."
  NOTE: Use the actual arrow character and em dash from the UI-SPEC, not ASCII approximations. The copy above uses -> and -- as placeholder; the executor MUST use the exact Unicode characters from the UI-SPEC copy: the right arrow in "0→1" and the em dash in "compound. Outside".
  - Body typography: text-[16px], font-normal, leading-[1.6]
  - Spacing: mt-4 below heading
  - Max prose width: consider max-w-[720px] for readability (narrower than full 1080px)
- Accept optional `className` prop

**CaseStudyCard.tsx** — Per UI-SPEC Component Inventory and Interaction States:
- Props: `{ title: string; description: string; href: string; className?: string }`
- Wrapped in Next.js `<Link>` component with the href — the entire card is clickable
- Card container: bg-neutral-100 (#F5F5F5), border border-neutral-200 (#E5E5E5), rounded-lg, p-6 (24px padding)
- Hover: `hover:bg-[#ebebeb]`. Active: `active:bg-[#e0e0e0]`. Focus: `focus-visible:outline-2 focus-visible:outline-neutral-900 focus-visible:outline-offset-2`
- Card title: `<h3>` with Heading typography (text-[20px], font-semibold, leading-[1.3])
- Card description: `<p>` with Body typography (text-[16px], leading-[1.6]), text-neutral-500 for muted color, mt-2
- "View case study" link text at bottom: text-[16px], font-normal, text-neutral-900, mt-4, `hover:underline`
  - This is visual text inside the card Link, not a separate link (the whole card is already a link)
- Cursor: pointer on the entire card (cursor-pointer on the Link)
- Accept optional `className` prop (merged onto outer element)

**WorkSection.tsx** — Per UI-SPEC:
- Semantic `<section>` with `aria-label="Work"`
- Padding: pt-12 pb-16 (48px top, 64px bottom)
- Content constrained to `max-w-[1080px] mx-auto`, horizontal padding px-4 sm:px-16
- `<h2>` with text "Work" — Heading typography: text-[20px], font-semibold, leading-[1.3]
- Grid of 4 CaseStudyCard components: `grid grid-cols-1 sm:grid-cols-2 gap-6`, mt-6 below heading
- Import CaseStudy type from `@/types` and CaseStudyCard from `@/components/ui/CaseStudyCard`
- Define case studies array inline (not fetched):
  ```
  1. { title: "Athlete Intelligence", description: "A performance and injury-risk decision system inside the FiyrPod Coach app", href: "/case-studies/athlete-intelligence" }
  2. { title: "Video Highlights with GPS Overlay", description: "Designed and shipped a video-to-data feature that increased engagement and drove organic growth", href: "/case-studies/video-highlights" }
  3. { title: "Breakthru CRM", description: "Designed and shipped a self-serve Community Hub builder, replacing a manual dev-dependent process", href: "/case-studies/breakthru-crm" }
  4. { title: "Breakthru Hub", description: "Redesigned engagement system for Breakthru CRM's customer community", href: "/case-studies/breakthru-hub" }
  ```
- Map over the array to render CaseStudyCard for each, using title as key
- Accept optional `className` prop

**page.tsx** — Replace placeholder content:
- Import HeroSection, AboutSection, WorkSection from `@/components/sections/*`
- Render inside `<main>`:
  ```
  <main>
    <HeroSection />
    <AboutSection />
    <WorkSection />
  </main>
  ```
- No additional wrapper divs. The Nav and Footer are already in layout.tsx.
- This is a Server Component (no "use client" directive needed — all components are static)
  </action>
  <verify>
    <automated>cd portfolio && npx tsc --noEmit && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - HeroSection renders h1 with exact headline text and p with exact tagline text
    - AboutSection renders h2 "About" and p with the full about body copy
    - WorkSection renders h2 "Work" and 4 CaseStudyCard components in a 2-col grid
    - CaseStudyCard renders title, description, "View case study" text, wrapped in Link
    - Case study hrefs are /case-studies/athlete-intelligence, /case-studies/video-highlights, /case-studies/breakthru-crm, /case-studies/breakthru-hub
    - page.tsx renders HeroSection, AboutSection, WorkSection inside main element
    - All sections use semantic HTML (section with aria-label, h1/h2/h3 hierarchy)
    - All components accept optional className prop
    - Mobile-first responsive: single column mobile, 2-col cards on sm breakpoint
    - Display text: 28px mobile, 40px desktop
    - No animations, no "use client" directives, no framer-motion
    - TypeScript compiles clean, production build succeeds
  </acceptance_criteria>
  <done>All section components render correct content with proper typography, spacing, and responsive layout. Page assembles all sections. Build passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of landing page</name>
  <files>portfolio/src/app/page.tsx</files>
  <action>
Present the completed landing page to the user for visual verification. Start the dev server if not running (`cd portfolio && npm run dev`). Guide the user through desktop and mobile checks per the how-to-verify steps below.
  </action>
  <what-built>Complete landing page with Nav, Hero, About, Work (4 case study cards), and Footer. Inter font applied. Design tokens from UI-SPEC implemented. Responsive layout (mobile-first, 2-col cards on desktop).</what-built>
  <how-to-verify>
    1. Run `cd portfolio && npm run dev` (if not already running)
    2. Open http://localhost:3000 in browser

    **Desktop (>=640px wide):**
    3. Nav: "Russ Seidametov" visible at top-left, sticky on scroll
    4. Hero: Large headline "Russ Seidametov. I turn complexity into clarity." (40px), smaller tagline below in muted gray
    5. About: "About" heading with full background paragraph
    6. Work: "Work" heading, 4 cards in 2-column grid. Each card shows title, description, "View case study"
    7. Footer: "Get in touch" with email and LinkedIn links, top border separator

    **Mobile (resize to ~375px):**
    8. Headline scales to 28px
    9. Cards stack to single column
    10. Padding tightens (16px sides vs 64px)
    11. All content readable, no horizontal overflow

    **Interactions:**
    12. Hover over a case study card — background darkens slightly
    13. Click a card — navigates to /case-studies/[slug] (404 is expected, route built in Phase 3)
    14. Tab through page — all links show visible focus outlines

    **Font:**
    15. Body text should be Inter (not Arial/system font). Check in DevTools: computed font-family should show Inter.
  </how-to-verify>
  <read_first>
    .planning/phases/02-landing-page/02-UI-SPEC.md (Layout Spec section — to compare against rendered output)
  </read_first>
  <verify>
    <automated>cd portfolio && npm run build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - All 5 page sections visible (Nav, Hero, About, Work, Footer)
    - Content matches UI-SPEC copywriting contract exactly
    - 2-col card grid on desktop, 1-col on mobile
    - Inter font is applied (visible in DevTools)
    - Hover states work on cards and links
    - Keyboard focus outlines visible on all interactive elements
    - No console errors
  </acceptance_criteria>
  <done>User has visually confirmed the landing page matches the UI-SPEC design contract on both desktop and mobile viewports</done>
  <resume-signal>Type "approved" or describe any issues to fix</resume-signal>
</task>

</tasks>

<verification>
After all tasks complete:

1. `cd portfolio && npm run build` — production build succeeds with zero errors
2. `cd portfolio && npx tsc --noEmit` — TypeScript compiles clean
3. Visual check: landing page renders all sections with correct content at localhost:3000
4. Responsive check: layout adapts correctly between mobile and desktop
5. No framer-motion, no CSS transitions, no animation classes in any file (grep confirms)
6. All components accept optional className prop (grep confirms)
7. Semantic HTML: nav, main, section (with aria-label), footer elements used
</verification>

<success_criteria>
- Landing page renders at localhost:3000 with Nav, Hero, About, Work (4 cards), Footer
- All copy matches UI-SPEC Copywriting Contract exactly
- Inter font loaded via next/font/google
- Design tokens (colors, spacing, typography) match UI-SPEC
- Responsive: mobile-first, 2-col cards at sm breakpoint, proper padding scaling
- Case study card links point to /case-studies/[slug] placeholder routes
- No animations or transitions (DESIGN-01 satisfied)
- All components accept className prop (DESIGN-03 satisfied)
- Production build passes clean
</success_criteria>

<output>
After completion, create `.planning/phases/02-landing-page/02-01-SUMMARY.md`
</output>
